{
    "$schema": "https://getcomposer.org/schema.json",
    "name": "sinemacula/coding-standards",
    "type": "phpcodesniffer-standard",
    "license": "Apache-2.0",
    "description": "Centralized coding standards, static analysis configurations, and code quality tooling for all Sine Macula repositories.",
    "authors": [
        {
            "name": "Ben Carey",
            "email": "bdmc@sinemacula.co.uk"
        }
    ],
    "require": {
        "php": "^8.3",
        "friendsofphp/php-cs-fixer": "^3.0"
    },
    "suggest": {
        "phpstan/phpstan-phpunit": "PHPUnit-aware PHPStan rules: data-provider validation, setUp()/tearDown() parent calls, assertSame discipline, and @covers / mock target existence. Auto-registers via phpstan/extension-installer.",
        "phpstan/extension-installer": "Auto-registers the shared PHPStan config and installed PHPStan extensions (required for the shared phpstan.neon to load)."
    },
    "autoload": {
        "psr-4": {
            "SineMacula\\CodingStandards\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "SineMacula\\Tests\\": "SineMacula/Tests/"
        },
        "classmap": [
            "SineMacula/Tests/PHPStan/data/readonly-class.inc",
            "SineMacula/Tests/PHPStan/data/redundant-static-return.inc"
        ]
    },
    "extra": {
        "phpstan": {
            "includes": [
                "php/phpstan-base.neon"
            ]
        }
    },
    "require-dev": {
        "squizlabs/php_codesniffer": "^3.13.6 || ^4.0.2",
        "slevomat/coding-standard": "^8.0",
        "dealerdirect/phpcodesniffer-composer-installer": "^1.0",
        "infection/infection": "^0.33.2",
        "phpunit/phpunit": "^12",
        "phpstan/phpstan": "^2.1",
        "phpstan/extension-installer": "^1.4",
        "phpstan/phpstan-phpunit": "^2.0",
        "symfony/console": "^7.4"
    },
    "config": {
        "allow-plugins": {
            "dealerdirect/phpcodesniffer-composer-installer": true,
            "infection/extension-installer": true,
            "phpstan/extension-installer": true
        }
    },
    "scripts": {
        "check": [
            "qlty check"
        ],
        "format": [
            "qlty fmt"
        ],
        "smells": [
            "qlty smells"
        ],
        "analyse": [
            "@php vendor/bin/phpstan analyse"
        ],
        "test": [
            "@php vendor/bin/phpunit"
        ],
        "test:coverage": [
            "@php -d pcov.enabled=0 -d xdebug.mode=coverage vendor/bin/phpunit --coverage-clover=clover.xml"
        ],
        "test:mutation": [
            "@php -d memory_limit=-1 -d pcov.enabled=0 -d xdebug.mode=coverage vendor/bin/infection --configuration=infection.json5 --only-covering-test-cases --threads=max --git-diff-lines --git-diff-base=origin/master --map-source-class-to-test --min-msi=90 --min-covered-msi=90 --ignore-msi-with-no-mutations"
        ],
        "test:mutation:full": [
            "@php -d memory_limit=-1 -d pcov.enabled=0 -d xdebug.mode=coverage vendor/bin/infection --configuration=infection.full.json5 --only-covering-test-cases --threads=max"
        ]
    },
    "scripts-descriptions": {
        "check": "Run static analysis and lint checks via qlty.",
        "format": "Format codebase using qlty.",
        "smells": "Report code smells (duplication, complexity) via qlty.",
        "analyse": "Run PHPStan over the repo's own sniffs and rule (phpstan.neon.dist).",
        "test": "Run the PHPUnit sniff test suite.",
        "test:coverage": "Run the PHPUnit sniff test suite with Clover coverage output (requires Xdebug).",
        "test:mutation": "Run the Infection mutation-testing gate against the MSI thresholds.",
        "test:mutation:full": "Run the full Infection mutation suite without MSI thresholds."
    }
}

<?php

namespace App;

// phpcs:disable SineMacula.Commenting.CommentLineLength
// This suppressed standalone comment is deliberately written to run far beyond the eighty character limit

// This suppressed run
// wraps far too early.
// phpcs:enable

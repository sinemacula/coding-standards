# This file was automatically generated by `qlty init`.
# You can modify it to suit your needs.
# We recommend you to commit this file to your repository.
#
# This configuration is used by both Qlty CLI and Qlty Cloud.
#
#     Qlty CLI -- Code quality toolkit for developers
#     Qlty Cloud -- Fully automated Code Health Platform
#
# Try Qlty Cloud: https://qlty.sh
#
# For a guide to configuration, visit https://qlty.sh/d/config
# Or for a full reference, visit https://qlty.sh/d/qlty-toml
config_version = "0"

exclude_patterns = [
    ".sinemacula/**",
    "SineMacula/Tests/**/*.inc",
    "SineMacula/Tests/**/*.inc.fixed",
    "*_min.*",
    "*-min.*",
    "*.min.*",
    "**/.yarn/**",
    "**/*.d.ts",
    "**/assets/**",
    "**/bower_components/**",
    "**/build/**",
    "**/cache/**",
    "**/config/**",
    "**/db/**",
    "**/deps/**",
    "**/dist/**",
    "**/extern/**",
    "**/external/**",
    "**/generated/**",
    "**/Godeps/**",
    "**/gradlew/**",
    "**/mvnw/**",
    "**/node_modules/**",
    "**/protos/**",
    "**/seed/**",
    "**/target/**",
    "**/templates/**",
    "**/testdata/**",
    "**/vendor/**",
]

test_patterns = [
    "**/test/**",
    "**/spec/**",
    # Match PHPUnit's PascalCase layout (Tests/, *Test.php); the defaults below
    # are lowercase-only, so without these qlty treats tests as source.
    "**/Tests/**",
    "**/*Test.php",
    "**/*.test.*",
    "**/*.spec.*",
    "**/*_test.*",
    "**/*_spec.*",
    "**/test_*.*",
    "**/spec_*.*",
]

[smells]
mode = "comment"

[[source]]
name = "default"
default = true

[runtimes.enabled]
php = "8.3"


[[plugin]]
name = "radarlint-php"
mode = "comment"

[[plugin]]
name = "ripgrep"
mode = "comment"

[[plugin]]
name = "trufflehog"

[[plugin]]
name = "editorconfig-checker"

[[plugin]]
name = "markdownlint"
mode = "comment"

# Dogfood this repo's own standards on its own code: phpcs and php-cs-fixer use
# the in-tree configs under .qlty/configs, not a released tag. slevomat comes
# from composer.json; the base class loads via .qlty/configs/autoload.php (qlty
# Cloud has no project vendor/autoload.php).
[[plugin]]
name = "php-codesniffer"
# Pinned to the 4.x major consumers run via qlty; the suite is tested on both
# 3.x and 4.x in CI (the Tests workflow's phpcs matrix). Must stay at or above
# 4.0.2: earlier 4.x is affected by a command-injection advisory that Composer
# refuses to install, which fails the plugin outright rather than reporting it.
version = "4.0.4"
mode = "comment"
package_file = "composer.json"
package_filters = ["slevomat", "dealerdirect"]

# phpcs 4.x exits 3 on errors (bitmask: 1 errors + 2 fixable), which qlty's
# built-in driver, written for 3.x exit semantics, treats as a plugin failure -
# silently discarding the findings. The self-lint resolves php-codesniffer from
# the default source (not this repo's source.toml), so the override that
# source.toml ships to consumers is mirrored here. 0/1/2/3 = "ran and found
# issues" on both majors; a real fatal exits higher (255) and still errors.
[plugins.definitions.php-codesniffer.drivers.lint]
success_codes = [0, 1, 2, 3]

# The installed version comes from composer.json, not from `version`. With a
# package_file and no package_filters, qlty installs that file's requirements,
# so friendsofphp/php-cs-fixer resolves from the constraint declared there and
# `version` only keys the tool sandbox. The two are kept in step deliberately:
# the floor in composer.json is what guarantees the version, and moving it
# without moving this leaves the sandbox keyed to a version nobody is running.
[[plugin]]
name = "php-cs-fixer"
version = "3.95.19"
package_file = "composer.json"

# phpstan is NOT wired here: it needs the project vendor (composer install) that
# qlty Cloud lacks. Run it locally with `composer analyse` (phpstan.neon.dist).

# php/ and .qlty/configs hold tool configs, not source, so the PHP plugins skip
# them; php/ stays so the cs-fixer config can require its rules file.
[[exclude]]
plugins = ["php-codesniffer", "php-cs-fixer"]
file_patterns = ["php/**", ".qlty/configs/**"]

# Release-please generates CHANGELOG.md with `*` bullets and long lines that
# markdownlint (MD004) and editorconfig-checker would reject. It is
# tool-authored, so exclude it from both (mirrors the source.toml excludes).
[[exclude]]
plugins = ["editorconfig-checker", "markdownlint"]
file_patterns = ["**/CHANGELOG.md"]

# The qlty-generated git hooks carry tab indentation; they are tool-authored, so
# their indent style is not ours to fix. Exclude them from editorconfig-checker.
[[exclude]]
plugins = ["editorconfig-checker"]
file_patterns = [".qlty/hooks/**"]
